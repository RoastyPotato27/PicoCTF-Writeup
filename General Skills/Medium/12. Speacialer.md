**Title:** Speacialer

**Difficulty:** Medium

**Summary (TL;DR):**
using command substitution in bash to replicate function of `ls` and `cat`.

**1. Scope & Setup**

**Target:** ctf-player@saturn.picoctf.net

**Environment:** Local System, picoVM

**Tools used:** Terminal

**2. Exploit**  

If you read my previous writeup of `Special` challenge, you will find a connection.

This challenge is not as bad as the previous one but the one who made these is a psycopath, they deleted ls and cat command...

So as an alternative, we will use command substitution to cat a file.

**what is command substitution?**

>**Command substitution** in Bash is a feature that **replaces `$(command)` with the output produced by that command**.

First of all, since we cant do ls, we will use `echo *` which does the exact same thing, it prints out everything in the current 
directory.

If we do that, we can now see that there are 3 more directories here, I went into them one by one, and found multiple .txt files.

Now, since I already messed around a bit to see which commands I can use, turns out cat is... NOT one of them.

So I did some reasearch to find other alternatives, 
like **less, more, head, tail, sed, awk, etc.** . Neither of which worked, of-course.

but there was also `echo$(<filename)` which, contained echo, which obviously works as we used it to list the directories.

So just replace 'filename' with the actual name of the file { ex. echo $(<lmao.txt) }, just rinse and repeat till you get 
to the file that has the flag, it will harly take a minute.

<img width="767" height="377" alt="image" src="https://github.com/user-attachments/assets/065322cd-fea2-4ee1-aadb-4887164cfd3a" />
<br></br>

**3. Flag**

`picoCTF{y0u_d0n7_4ppr3c1473_wh47_w3r3_d01ng_h3r3_838b49d1}`
