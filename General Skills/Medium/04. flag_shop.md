**Title:** flag_shop

**Difficulty:** Medium

**Summary (TL;DR):**
using stack overflow to manipulate the values.

**1. Scope & Setup**

**Target:** fickle-tempest.picoctf.net

**Environment:** Local System, picoVM

**Tools used:** Terminal,python

**2. Recon**

**What is Stack Overflow?**

>A **stack overflow** occurs when a program uses more stack memory than allocated—commonly due to deep/uncontrolled recursion or writing past a fixed-size stack buffer (e.g., overflowing `char buf[64]`)—causing memory corruption or a crash.

**3. Exploit**

Once we read the the c source file, we will unerstand whats happenning here. Our goal is to make the system glitch out 
coz it expects a fixed size of input, and doesnt have preventive measures if someone tried to enter a very large value.

After reading the code, I realised that I could possibly make it overflow, but I was trying to do it to the wrong variable, HAHA.

After miserably failing and not knowing why I was failing, I went to check online, and my method was right but the target
was wrong. We essentially need to overflow the `account_balance` integer. 

Our account banalce is 1100 when we start, which is obviously not enough to buy the real flag.

So, if we try to buy the first flag in the list, the fake one, and buy more that what our account balance allows, 
it will go to negative. Which shouldn't normally happen. 

Good for us! 

<img width="489" height="456" alt="image" src="https://github.com/user-attachments/assets/306302ce-b92a-4e0f-a067-8b2bbc044af3" />

Well now that our infinite money glitch is working, we will use it to buy the real flag (option 2) without any problem.

**4. Flag**

`picoCTF{m0n3y_bag5_4E85dfe5}`


