**Title:** Based

**Difficulty:** Medium

**Summary (TL;DR):**
Using Python script to decode Binary, Hex and Octa.

**1. Scope & Setup**

**Target:** fickle-tempest.picoctf.net

**Environment:** Local System, picoVM

**Tools used:** Terminal,python

**2. Recon**

I connected and checked what all encodings were used. Turns out its **binary > octa > hexa**

**3. Exploit**

Heres the code i wrote to 

>  text = input("string: ")
> 
>   def detect_type(st):
> 
>      st_clean = st.replace(' ', '').lower()
>        
>      if all(c in '01' for c in st_clean):
>          print("Its binary.\n")
>          print(binary_to_text(st_clean))
>  
>      elif all(c in '0123456789abcdef' for c in st_clean):
>          print("Its hex.\n")
>          print(hex_to_text(st_clean))
>  
>      elif all(c in '01234567o' for c in st_clean):
>          print("Its octal.\n")
>          print(octal_to_text(st))
>  
>      else:
>          print("Unknown format.")
>  
>  def binary_to_text(st):
> 
>      chars = [st[i:i+8] for i in range(0, len(st), 8)]
>      return ''.join(chr(int(char, 2)) for char in chars)
>  
>  def hex_to_text(st):
> 
>      return bytes.fromhex(st).decode("utf-8")
>  
>  def octal_to_text(st):
> 
>      parts = st.split()
>      text = ''
>      for p in parts:
>          if p.startswith('o'):
>              text += chr(int(p[1:], 8))
>          else:
>              text += chr(int(p, 8))
>      return text
>  
>  detect_type(text)

**4. Flag**

`picoCTF{learning_about_converting_values_DF19A0E8}`
