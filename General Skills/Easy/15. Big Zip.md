**Title:** Big Zip

**Difficulty:** Easy

**Summary (TL;DR):**

Using our handy dandy Grep tool to search through loads of files for a keyword.

Run  `grep -r "picoCTF" big-zip-files/`

**1. Scope & Setup**

**Target:** --

**Environment:** Local System, pico VM

**Tools used:** Terminal

**2. Recon**

>**What is Grep?**
>
>**grep** is a command-line tool that searches text for lines matching a given pattern (usually a regular expression).

Run the following command outside the **main** challenge directory, or just copy pasta the command

`grep -r "picoCTF" big-zip-files/`

<img width="801" height="122" alt="image" src="https://github.com/user-attachments/assets/6cf4d9e5-e245-45d9-bb60-e4216296792e" />

**3.Flag**

`picoCTF{gr3p_15_m4g1c_ef8790dc}`
