**Title:** Bytemancy 1

**Difficulty:** Easy

**Summary (TL;DR):**

using a script to spam eeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee

**1. Scope & Setup**

**Target:** --

**Environment:** Local System, PicoVM

**Tools used:** Terminal, bash

**2. Exploit**

so once you read the python file provided in the challenge you will see that its asking us to spam e (in 1 line, no space) 1751 times.

So to automate this task, I wrote this bash script:

<img width="499" height="227" alt="image" src="https://github.com/user-attachments/assets/434c8d9c-7f90-4b0e-b4eb-025e8e578cd0" />
<br></br>

So whats going on in this script?
xdotool key ctrl+shift+v

We declared that this is a bash script by using `#! /usr/bin/env bash`

The `COUNT` varaible stores the number of times we have to spam.  

`for i in $(seq 1 $COUNT); do` , this is a loop which will go from 1 till whatever number is in our count varaible and do stuff.

`echo -n  "$MSG" | xclip -selection clipboard`, here the script will print the message which is "e" in this case. -n will tell
echo not to create newline. "xclip" is a tool that I downloaded to copy something from terminal(/script) to my linux clipboard.

`xdotool key ctrl+shift+v` xdotool is another tool which simulates keyboard and mouse in linux

So xclip will copy my message to clipboard, and xdotool will paste it into whatever program I want it to.

**3. Flag**

`picoCTF{h0w_m4ny_e's???_706320e0}`
