**Title:** Commitment Issue

**Difficulty:** Easy

**Summary (TL;DR):**

Learning the basics of Git Version Control.

**1. Scope & Setup**

**Target:** --

**Environment:** Local System / PicoCTF VM

**Tools used:** Terminal, Browser

**2. Recon**

**What is Git's Version Control?**
>  Git version control is a system that tracks changes to files over time, allowing multiple people to collaborate and manage code efficiently.

**3. Exploit**

From reading the hints, we get redirected to `https://primer.picoctf.org/#_git_version_control` 

Here we get 3 useful commands: `git init`, `git log` and `git checkout <ID>`

First of all download and extract the challenge.zip. Use your terminal and go into the "drop-in" directory. 
use to "git init" command to reinstall the git environment here. 

Use "git log" and u will see different commits. These commits are essentially snapshots of timeline.

use "git checkout" followed by the commit ID, to jump into one of those snapshots of the past. 

Now that u have successfully time-traveled, cat the .txt file to get your flag!

**4. Flag**

`picoCTF{s@n1t1z3_9539be6b}`
